export function isLoggedIn(state) {
  return state.isLoggedIn;
}

export function profile(state) {
  return state.profile;
}

export function autoUpload(state) {
  return state.autoUpload;
}

export function uploadThreads(state) {
  return state.uploadThreads;
}

export function hashingThreads(state) {
  return state.hashingThreads;
}

export function desktopSplitter(state) {
  return state.desktopSplitter;
}

export function mobileSplitter(state) {
  return state.mobileSplitter;
}
